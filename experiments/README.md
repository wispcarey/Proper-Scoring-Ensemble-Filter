# Experiments

This directory documents the main paper experiment families. The runnable scripts remain in the repository root and `scripts/`.

Generated datasets, checkpoints, cached benchmark results, and figures are expected under ignored local directories such as `data/` and `save/`.
